const headers = {
  Accept: "application/json",
  "Content-Type": "application/json",
};
const url

const getAllAdUnits = (params, forcedSync) => {
  params.forcedSync = forcedSync;
  return {
    url: ,
    headers,
    params: {
      filter: params,
    },
    method: "GET",
  };
};
